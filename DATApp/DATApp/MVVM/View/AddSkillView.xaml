<Window x:Class="DATApp.MVVM.View.AddSkillView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DATApp.MVVM.View"
        xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
        mc:Ignorable="d"
        Width="300"
        Height="550">
    
    <Window.DataContext>
        <viewModel:SkillsViewModel/>
    </Window.DataContext>
    
    <Grid Margin="20,20,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="450"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Stretch">
            <Label Content = "Navn" />
            <TextBox Text = "{Binding Name}" />

            <Label Content="Purpose" />
            <TextBox Text = "{Binding Purpose}" />

            <Label Content="Description" />
            <TextBox Text = "{Binding Description}" />

            <TextBlock Text="Module" />
            <ComboBox SelectedItem="{Binding Module}" ItemsSource="{Binding ModulesCollectionView, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" />


            <!-- Checkboxes            
            
            <TextBlock Text="Level" />
            <ComboBox SelectedItem="{Binding Level}" ItemsSource="{Binding Levels, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" />

            <TextBlock Text="Emotion" />
            <ComboBox SelectedItem="{Binding EmotionMatch}" ItemsSource="{Binding Emotions, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" />

           

            <CheckBox Content="Vrede" x:Name="VredeCheckbox"/>
            <CheckBox Content="Aggreession" x:Name="AggressionCheckbox"/>
            <CheckBox Content="Nedtrykthed" x:Name="NedtrykthedCheckbox"/>
            <CheckBox Content="Angst" x:Name="AngstCheckbox"/>
            <CheckBox Content="Skyld" x:Name="SkyldCheckbox"/>
             -->

        </StackPanel>

        <Button Grid.Row="1" Height="20" Width="100" Content="Add Skill" Command="{Binding AddSkillCommand}" />

    </Grid>
   
</Window>
