<Window x:Class="DATApp.MVVM.View.AddSkillView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DATApp.MVVM.View"
        xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
        mc:Ignorable="d"
        Width="300"
        Height="350">
    
    <Window.DataContext>
        <viewModel:SkillsViewModel/>
    </Window.DataContext>
    
    <Grid Margin="20,20,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height=" 200"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Stretch">
            <Label Content = "Navn:" />
            <TextBox Text = "{Binding Name}" />
            <Label Content = "Description:" />
            <TextBox Text = "{Binding Description}" />
        </StackPanel>

        <Button Grid.Row="1" Height="30" Width="100" Content="Add Module" Command="{Binding AddSkillCommand}" />

    </Grid>
   
</Window>
