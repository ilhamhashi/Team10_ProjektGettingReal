<UserControl x:Class="DATApp.MVVM.View.AdminSkillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
             
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:SkillsViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="30*"/>
        </Grid.ColumnDefinitions>

        <!-- Search Section 

        <StackPanel Orientation="Horizontal" Margin="0,10" Grid.Row="1">
            <TextBox Width="300"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Search"
                    Width="100"
                    Height="30"
                    Command="{Binding SearchCommand}" />
        </StackPanel>

        Add Section 
        <GroupBox Header="Tilføj Færdighed" Margin="0,10,0,10" Grid.Row="2">
            <StackPanel Margin="10" Orientation="Vertical">
                <TextBlock Text="Navn" />
                <TextBox Text="{Binding NewSkillName}" Margin="0,5,0,10" />

                <TextBlock Text="Beskrivelse" />
                <TextBox Text="{Binding NewSkillDescription}" Margin="0,5,0,10" />

                Der er noget galt her, NewSkillLevel binding burde være noget andet!!!!

                <TextBlock Text="Følelse" />
                <ComboBox SelectedItem="{Binding NewSkillLevel}" 
          ItemsSource="{Binding EmotionalStates}" 
          Margin="0,5,0,10" />

                <TextBlock Text="Niveau" />
                <ComboBox SelectedItem="{Binding NewSkillLevel}" 
          ItemsSource="{Binding Levels}" 
          Margin="0,5,0,10" />


                <Button Content="Add Skill"
                        Width="120"
                        Margin="0,10,0,0"
                        Command="{Binding AddCommand}" />
            </StackPanel>
        </GroupBox> -->

        <!-- Search TextBox -->
        <TextBox Grid.Row="0" Height="35" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="20,10,30,0"/>


        <!-- Results List with ScrollViewer -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding SkillsCollectionView, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedSkill, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Stretch"
                     Margin="20,10,30,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5" Orientation="Vertical">
                            <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" FontStyle="Italic" FontSize="12" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Number: " FontSize="12" />
                                <TextBlock Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" FontSize="12" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Module: " FontSize="12" />
                                <TextBlock Text="{Binding Module.Name, UpdateSourceTrigger=PropertyChanged}" FontSize="12" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Level: " FontSize="12" />
                                <TextBlock Text="{Binding Level, UpdateSourceTrigger=PropertyChanged}" FontSize="12" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <controls:AdminEditSkillPanel
            Grid.Row="1"
            Grid.Column="1"/>

        
    </Grid>
</UserControl>
