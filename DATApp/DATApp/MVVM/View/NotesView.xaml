<UserControl x:Class="DATApp.MVVM.View.NotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:viewmodel="clr-namespace:DATApp.MVVM.ViewModel"             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodel:NotesViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding NotesCollectionView, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedNote, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Stretch"
                     Margin="20,10,30,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5" Orientation="Vertical">
                            <TextBlock Text="{Binding Skill.Name, UpdateSourceTrigger=PropertyChanged}" FontWeight="Light" />
                            <TextBlock Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" FontWeight="Light" />
                            <TextBlock Text="{Binding DateTime, UpdateSourceTrigger=PropertyChanged}" FontStyle="Italic" FontSize="12" />
                            <TextBlock Text="{Binding Client.Name, UpdateSourceTrigger=PropertyChanged}" FontStyle="Italic" FontSize="12" />

                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <StackPanel  Grid.Row="1" Margin="20,10,30,0">
            <TextBlock Text="Note Content:"/>
            <TextBox Text="{Binding SelectedNote.Content, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
            <Button Content="Delete Note" Command="{Binding DeleteNoteCommand}" Width="100" Margin="5"/>
            <Button Content="Save Changes" Command="{Binding SaveNoteCommand}" Width="100" Margin="5"/>
        </StackPanel>

    </Grid>
</UserControl>
