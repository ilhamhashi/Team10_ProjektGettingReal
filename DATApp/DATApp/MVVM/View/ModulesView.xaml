<UserControl x:Class="DATApp.MVVM.View.ModulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"            
             xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <viewModel:ModulesViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBox Height="35" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="20,10,30,0"/>

        <!-- Results List with ScrollViewer -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding ModulesCollectionView}"
                     SelectedItem="{Binding SelectedModule}"
                     HorizontalAlignment="Stretch"
                     Margin="20,10,30,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5" Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Description}" FontStyle="Italic" FontSize="12" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Nummer: " FontSize="12" />
                                <TextBlock Text="{Binding ModuleNumber}" FontSize="12" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
        <Label Content = "Navn:" />
        <TextBox Width="200" Height="35" Text = "{Binding SelectedModule.Name, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content = "Description:" />
        <TextBox Width="200" Height="35" Text = "{Binding SelectedModule.Description, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save Module" Command="{Binding SaveModuleCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Delete Module" Command="{Binding DeleteModuleCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Add Module" Command="{Binding OpenAddModuleCommand}" Width="100" Height="30"/>
        </StackPanel>

    </Grid>
</UserControl>
