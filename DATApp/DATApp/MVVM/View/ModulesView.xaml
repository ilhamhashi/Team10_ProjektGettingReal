<UserControl x:Class="DATApp.MVVM.View.ModulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    
    <UserControl.DataContext>
        <viewModel:ModulesViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid>
            <ListView x:Name="ModuleList" ItemsSource="{Binding Modules, UpdateSourceTrigger=Explicit}" SelectedItem="{Binding SelectedModule}" BorderBrush="#cdcdcd" Margin="25,70,399,125" Padding="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Description"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>
        <Label Content = "Navn:" Margin="453,143,16,281" />
        <TextBox Text = "{Binding SelectedModule.Name, UpdateSourceTrigger=PropertyChanged}" Margin="453,169,16,263" />
        <Label Content = "Description:" Margin="453,187,16,237" />
        <TextBox Text = "{Binding SelectedModule.Description, UpdateSourceTrigger=PropertyChanged}" Margin="453,213,16,219" />
        

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Save Module" Command="{Binding SaveModuleCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Delete Module" Command="{Binding DeleteModuleCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Add Module" Command="{Binding OpenAddModuleCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
        </StackPanel>

    </Grid>
</UserControl>
