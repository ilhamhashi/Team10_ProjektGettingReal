<Window x:Class="DATApp.MVVM.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DATApp.MVVM.View"
        xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
        xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Height="400" Width="300"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <viewModel:UsersViewModel />
    </Window.DataContext>

    <Border Background="AliceBlue"
        CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="400"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <controls:EmailTextBox 
                Margin="0,116,0,232"/>
            <controls:AdgangskodeTextBox Margin="0,172,0,172"/>

            <Label Content="Log ind" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" FontFamily="Leelawadee UI Semilight" FontSize="24" Foreground="#FF18025F"/>
            <Label Content="Kontakt klinikken for oprettelse af bruger" HorizontalAlignment="Center" Margin="0,228,0,148" FontFamily="Calibri Light" Foreground="#FF18025F" FontSize="10"/>

            <Button Name = "CloseButton" Content = "Luk" Click = "CloseButton_Click" Margin = "130,349,130,21"/>
            <Button x:Name = "FortsætLogin" Content = "Fortsæt" Command="{Binding ValidateUserLoginCommand}" Margin = "118,281,118,89"/>

        </Grid>
    </Border>
</Window>
 