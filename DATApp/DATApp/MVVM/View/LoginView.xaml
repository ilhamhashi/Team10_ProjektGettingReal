<Window x:Class="DATApp.MVVM.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DATApp.MVVM.View"
        xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
        xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Height="400" Width="300"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.DataContext>
        <viewModel:UsersViewModel />
    </Window.DataContext>

    <Border Background="AliceBlue"
            CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="130"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Log ind" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" FontFamily="Leelawadee UI Semilight" FontSize="24" Foreground="#FF18025F"/>

            <StackPanel Grid.Row="1">
                <controls:EmailTextBox Grid.Row="1" Margin="0,0,0,20"/>
                <controls:AdgangskodeTextBox Grid.Row="2"/>
            </StackPanel>
            
            <StackPanel Grid.Row="2">
                <Label Content="Kontakt klinikken for oprettelse af bruger" HorizontalAlignment="Center" FontFamily="Calibri Light" Foreground="#FF18025F" FontSize="10"  Margin="0,0,0,20"/>
                <Button Name = "CloseButton" Content = "Luk" Click = "CloseButton_Click" Width="150" Margin="0,0,0,10"/>
                <Button x:Name = "FortsætLogin" Content = "Fortsæt" Command="{Binding ValidateUserLoginCommand}" Width="150"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
 