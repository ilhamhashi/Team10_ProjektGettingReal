<UserControl x:Class="DATApp.MVVM.View.SkillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModel:SkillsViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <!-- This row takes the remaining space for ListBox -->
        </Grid.RowDefinitions>

        <!-- Search Section -->
        <TextBox Grid.Row="0" Height="35" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="20,10,30,0"/>
    
        <!-- Results List with ScrollViewer -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding SkillsCollectionView, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedSkill, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Stretch"
                     Margin="20,10,30,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5" Orientation="Vertical">
                            <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold" />
                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" FontStyle="Italic" FontSize="12" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Nummer: " FontSize="12" />
                                <TextBlock Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}" FontSize="12" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Label Content = "Navn:" />
            <TextBox Width="200" Height="35" Text = "{Binding SelectedSkill.Name, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content = "Beskrivelse:" />
            <TextBox Width="200" Height="35" Text = "{Binding SelectedSkill.Description, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>


        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Gem færdighed" Command="{Binding SaveSkillCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Slet færdighed" Command="{Binding DeleteSkillCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Tilføj Færdighed" Command="{Binding OpenAddSkillCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>
