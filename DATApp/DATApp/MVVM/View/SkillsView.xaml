<UserControl x:Class="DATApp.MVVM.View.SkillsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Set the DataContext to the SkillsViewModel -->
    <UserControl.DataContext>
        <vm:SkillsViewModel />
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Skills Search"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="Black"
                   Margin="0,0,0,10" />

        <!-- Search Section -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,10">
            <TextBox Width="300"
                     Height="30"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />

            <Button Content="Search"
                    Width="100"
                    Height="30"
                    Command="{Binding SearchCommand}" />

            <Button Content="Add"
                    Width="100"
                    Height="30"
                    Margin="10, 0, 0, 0"
                    Command="{Binding AddCommand}" />
        </StackPanel>

        <!-- Results List -->
        <ListBox Grid.Row="2" ItemsSource="{Binding SearchResults}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding Description}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Category}" FontStyle="Italic" FontSize="12"/>
                        <TextBlock Text="ID: {Binding ID}" FontSize="12"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
