<UserControl x:Class="DATApp.MVVM.View.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:UsersViewModel/>
    </UserControl.DataContext>

    <Grid>
        
        <Grid Background="White" >
            <ListView x:Name="UserList" ItemsSource="{Binding Users, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedUser}" BorderBrush="#cdcdcd" Margin="25,70,399,125" Padding="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email"/>
                        <GridViewColumn DisplayMemberBinding="{Binding IsAdmin}" Header="Admin"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>
        <Label Content = "Navn:" Margin="453,143,16,281" />
        <TextBox Text = "{Binding SelectedUser.Name, UpdateSourceTrigger=PropertyChanged}" Margin="453,169,16,263" />
        <Label Content = "E-mail:" Margin="453,187,16,237" />
        <TextBox Text = "{Binding SelectedUser.Email, UpdateSourceTrigger=PropertyChanged}" Margin="453,213,16,219" />
        <Label Content = "Adgangskode:" Margin="453,231,16,195" />
        <TextBox Text = "{Binding SelectedUser.Password, UpdateSourceTrigger=PropertyChanged}" Margin="453,260,16,172"/>
        <TextBlock Text="Brugertype" Margin="453,279,16,156" />
        <ComboBox x:Name="BrugerTypeDropDown" SelectedValue="{Binding SelectedUser.IsAdmin, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Padding="2" Margin="453,300,16,0" Height="22" VerticalAlignment="Top">
            <ComboBoxItem Content="Klient bruger" Tag="False"/>
            <ComboBoxItem Content="Admin bruger" Tag="True"/>
        </ComboBox>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Save User" Command="{Binding SaveUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Add User" Command="{Binding OpenAddUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
        </StackPanel>

    </Grid>

</UserControl>
