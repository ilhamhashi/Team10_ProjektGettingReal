<UserControl x:Class="DATApp.MVVM.View.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:UsersViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <controls:ClearableTextBox Grid.Row="0" Width="200" Height="35" HorizontalAlignment="Left" Margin="20,0,0,0" Placeholder="Søg"/>
            <Button Grid.Row="0" Width="35" Height="35" Content="Søg"/>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="UserList" ItemsSource="{Binding Users, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedUser}" BorderBrush="#cdcdcd">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email"/>
                        <GridViewColumn DisplayMemberBinding="{Binding IsAdmin}" Header="Admin"/>
                    </GridView>
                </ListView.View>
            </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Label Content = "Navn:" />
            <TextBox Width="150" Height="35" Text = "{Binding SelectedUser.Name, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content = "E-mail:" />
            <TextBox Width="150" Height="35" Text = "{Binding SelectedUser.Email, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content = "Adgangskode:" />
            <TextBox Width="150" Height="35" Text = "{Binding SelectedUser.Password, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Brugertype" Margin="453,279,16,156" />
            <ComboBox x:Name="BrugerTypeDropDown" SelectedValue="{Binding SelectedUser.IsAdmin, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Padding="2" VerticalAlignment="Top">
                <ComboBoxItem Content="Klient bruger" Tag="False"/>
                <ComboBoxItem Content="Admin bruger" Tag="True"/>
            </ComboBox>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Save Changes" Command="{Binding SaveUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
            <Button Content="Add User" Command="{Binding OpenAddUserCommand, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="30"/>
        </StackPanel>

    </Grid>

</UserControl>
