<UserControl x:Class="DATApp.MVVM.View.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:controls="clr-namespace:DATApp.MVVM.View.Controls"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:UsersViewModel/>
    </UserControl.DataContext>

    <StackPanel Margin = "20" >
        <Grid>
            <Grid Background="White" Margin="25,71,0,-179" >
                <Button Height="30" Command="{Binding AddUserCommand}"  Content="Tilføj bruger" Width="100" VerticalContentAlignment="Center" Padding="5" Margin="0,50,121,0" VerticalAlignment="Top" HorizontalAlignment="Right" BorderThickness="1" BorderBrush="#E0E0E0" RenderTransformOrigin="-1.669,7.553"  />
                <ListView x:Name="UserList" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" BorderBrush="#cdcdcd" Margin="25,70,321,25" Padding="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name"/>
                            <GridViewColumn DisplayMemberBinding="{Binding Email}" Header="Email"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button Height="30"  Content="Slet Bruger" Width="100" VerticalContentAlignment="Center" Padding="5" Margin="0,258,189,0" VerticalAlignment="Top" HorizontalAlignment="Right" BorderThickness="1" BorderBrush="#E0E0E0" RenderTransformOrigin="-1.669,7.553"  />
                <Button Height="30"  Content="Gem Bruger" Width="100" VerticalContentAlignment="Center" Padding="5" Margin="0,258,50,0" VerticalAlignment="Top" HorizontalAlignment="Right" BorderThickness="1" BorderBrush="#E0E0E0" RenderTransformOrigin="-1.669,7.553" Click="Button_Click_1"  />
                <controls:Søgeknap Margin="10,15,338,248"/>
            </Grid>
            <Label Content = "Navn:" Margin="453,143,16,36" />
            <TextBox Text = "{Binding SelectedUser.Name, UpdateSourceTrigger=PropertyChanged}" Margin="453,169,16,18" />
            <Label Content = "E-mail:" Margin="453,187,16,-8" />
            <TextBox Text = "{Binding SelectedUser.Email, UpdateSourceTrigger=PropertyChanged}" Margin="453,213,16,-26" />
            <Label Content = "Adgangskode:" Margin="453,231,16,-52" />
            <TextBox Text = "{Binding SelectedUser.Password, UpdateSourceTrigger=PropertyChanged}" Margin="453,257,16,-70" />
            <TextBlock Text="Brugertype" Margin="453,279,16,-90" />
            <ComboBox x:Name="BrugerTypeDropDown"  SelectedIndex="0" Padding="2" Margin="453,300,16,-115">
                <ComboBoxItem Content="Klient bruger"/>
                <ComboBoxItem Content="Admin bruger"/>
            </ComboBox>

        </Grid>


    </StackPanel >

</UserControl>
