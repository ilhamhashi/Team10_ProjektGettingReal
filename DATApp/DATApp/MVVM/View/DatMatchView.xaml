<UserControl x:Class="DATApp.MVVM.View.DatMatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DATApp.MVVM.View"
             xmlns:viewModel="clr-namespace:DATApp.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="AliceBlue">
    <UserControl.DataContext>
        <viewModel:SkillsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Margin="30,0,30,0">
            <Label Content="DAT-MATCH" FontSize=" 24" VerticalAlignment="Top" Foreground="Black"/>
            <TextBlock Text="Vælg en følelse" />
            <ComboBox SelectedItem="{Binding EmotionMatch}" ItemsSource="{Binding Emotions, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" />
            <TextBlock Text="Vælg en intensitet" />
            <ComboBox SelectedItem="{Binding Level}" ItemsSource="{Binding Levels, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" />
        </StackPanel>
    </Grid>
</UserControl>
